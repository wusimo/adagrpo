# Flow-matching policy
name: flow_matching
type: flow

hidden_dim: 256
num_inference_steps: 10
sigma_min: 1e-4

il:
  lr: 1e-4
  weight_decay: 1e-6
  num_epochs: 100
  batch_size: 256
  aln_lr: 3e-4
  aln_warmup_steps: 500
